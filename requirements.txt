[phases.setup]
# (optional) install OS-level deps here

[phases.install]
run = "pip install -r requirements.txt"

[phases.build]
run = "echo Build complete"

[phases.start]
run = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[env]
PYTHON_PACKAGE = "python310"
